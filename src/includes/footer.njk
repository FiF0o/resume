<footer class="footer">
    <div class="container utl-space-between">
        <p>&copy; {{ author.name }}</p>
        <p>{{ 'labels.lastUpdated' | i18n }}: {{ build.timestamp | dateToFormat('dd.MM.yyyy') }}</p>
        {% if build.demo %}
            <a href="https://github.com/maxboeck/resume" class="gh-link utl-screen-only" target=”_blank”>
                <span>{% icon "github" %} view source</span>
            </a>
        {% endif%}
    </div>
</footer>